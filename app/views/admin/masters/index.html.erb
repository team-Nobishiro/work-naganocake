<div class="container">
  <dic class="row">
  	<h3>管理者画面</h3>
  	本日の注文件数<%= link_to admin_order_items_path do %>
  	<%= Order.where(created_at: Time.zone.now.all_day).count %><% end %>件
  </dic>
</div>